language: julia
os:
    - osx
julia:
    - release
    - nightly
notifications:
    email: false
after_success:
    - julia -e 'cd(Pkg.dir("Conda")); Pkg.add("Coverage"); using Coverage; Coveralls.submit(Coveralls.process_folder())'
